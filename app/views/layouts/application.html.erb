<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Blog Bootcamp" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Blog Bootcamp">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <nav class="bg-white shadow-md">
      <div class="max-w-7xl mx-auto px-4 py-4 flex items-center justify-between">
        <div class="flex gap-4">
          <%= link_to "Home", root_path, class: "text-blue-600 hover:text-blue-800 font-semibold" %>
          <% if user_signed_in? %>
            <%= link_to "New Post", new_post_path, class: "text-blue-600 hover:text-blue-800 font-semibold" %>
          <% end %>
        </div>
        <div class="flex gap-4 items-center">
          <% if user_signed_in? %>
            <span class="text-gray-600"><%= current_user.name %></span>
            <% if current_user.admin? %>
              <span class="text-xs bg-red-100 text-red-800 px-2 py-1 rounded">Admin</span>
            <% end %>
            <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "text-red-600 hover:text-red-800 font-semibold" %>
          <% else %>
            <%= link_to "Sign In", new_user_session_path, class: "text-blue-600 hover:text-blue-800 font-semibold" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "text-blue-600 hover:text-blue-800 font-semibold" %>
          <% end %>
        </div>
      </div>
    </nav>

    <div class="max-w-7xl mx-auto px-4 py-8">
      <% flash.each do |key, message| %>
        <div class="mb-4 p-4 rounded-lg <%= key == 'notice' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>">
          <%= message %>
        </div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
